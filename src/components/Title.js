import React from "react";
import styled from 'styled-components';

const Title = () => (
  <>
    <span>yves</span>
    <Suffix>gurcan</Suffix>
  </>
);

const Suffix = styled.span`
  color: gray;
`;

export default Title;